INSTRUCTIONS

- Start your apache and mysql database in xampp
- import the localhost.sql into your database
- 
- open a terminal in the mysql & hcmApp folder and run npm install
- Open mysql folder and run the command "nodemon server.js" to start and run the database
- Go into the hcmApp and run the angular project "ng serve"



credentials (username, password)
 => Emp65294, password
 => H2020435, password